package org.mubin.DataStructures;
//23

public class Trees {
    //                  Tree:
    //      tree = a non-linear data structure where nodes are organized in a hierarchy

    // * File Explorer
    // * Databases
    // * DNS
    // * HTML DOM

    // Root>Branch>Leaf (GrandParents>Parents/Child(GrandParent's Child)>Child)

    //      subtree = a smaller tree held within a larger tree
    //      size = number of nodes
    //      depth = number of edges below the root node
    //      height = number of edges above furthest leaf(child) node



}
